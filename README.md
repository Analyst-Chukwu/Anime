# Anime
#**Data Cleaning in SQL**: Moved all data to their appropriate columns using LIKE operator, wildcard character and CASE statement.

Formatted columns to fit into their right formats.

Dropped irrelevant columns.

Use of SUBSTRINGE and PARSENAME function to split columns.

Use of CONCAT() function to merge appropriate columns.

Checked data for dupplicates with use of ROW-NUMBER() function, and PARTITION BY clause.

Checked data for NULL values and updated where neccesairly.


